package org.example.companies;

public class Singulr {
    // 10 July, 2026
    // a schedular which accepts the job which will run at perticalar time
    // once job execution is done, return list of all jobs
    // daily 9pm job, example: do analytics at 9pm and send mail to 5 user at 10 pm
    // return that 5 job and run those

    // Schedule Job -> Email JOb, Analytics JOb
    // id, executionTime, status
    // get/calculateChildJob, execute

    // Job, id, name
    // Conditions =>  $.eventTyoe  = "OrderComplete"

    // Job Scheduler
    // schedule(), start(), cancel(), getNextJob()

    // In memory Job schedular
    // Queue<> jobQueue;
    // ExecutorService jobExecuter;
    // ScheduledExecutorService schedule
    // scheduler.schedule(every 5 secs.)



    // job.schedule(),
    // jobQueue.offer(jobId)

}
